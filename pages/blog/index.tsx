// for nested routes - we make related folder in pages and for main page named index.tsx
// whereas sub route named as the route.

const blog = () => {
    return <h1> Blog main page , main blog route </h1>
}

export default blog;
